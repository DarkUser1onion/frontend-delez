# TEST_PLAN.md – Delёz v0.1.0 (десктоп + веб + API)

## 1. Что проверяется
- Десктоп-клиент: установщики .msi (Win), .dmg (macOS), .AppImage (Linux), .deb (Linux), .rpm (Linux), Universal setup (Linux)
- Веб-версия: https://delez.tech
- API: https://api.delez-repo.ru
- GitHub Release: https://github.com/DarkUser1onion/frontend-delez/releases/tag/v0.1.0
## 2. Сценарии тестирования (12 проверок)

| ID | Сценарий | Ожидаемый результат | Инструмент | Статус |
|----|----------|---------------------|------------|--------|
| TC-01 | Установка .msi на Windows | Установка завершена, ярлык появился | Ручная проверка | PASS |
| TC-02 | Установка .dmg на macOS | Приложение скопировано в /Applications, после `xattr -d com.apple.quarantine` запускается | Терминал | PASS |
| TC-03 | Запуск .AppImage на Linux | Выданы права `chmod +x`, запуск без ошибок | Терминал | PASS |
| TC-04 | Запуск приложения, проверка иконки в трее | На Windows – иконка .ico, на Linux – PNG, отображается корректно | Визуально | PASS |
| TC-05 | Авторизация (любой логин/пароль) | Вход выполнен, нет CORS-ошибок в консоли | DevTools Console | PASS |
| TC-06 | Отправка сообщения в чат с ИИ | Ответ приходит потоково (SSE), ошибок нет | DevTools Network | PASS |
| TC-07 | Голосовой ввод на Linux | Микрофон работает, текст распознаётся (исправлен выбор arecord) | UI + логи | PASS |
| TC-08 | Закрытие окна через крестик / Cmd+W / Alt+F4 | Приложение уходит в трей, кнопка в меню меняется на "Открыть" | UI | PASS |
| TC-09 | Открытие окна из трея (клик по иконке или "Открыть") | Окно появляется, кнопка снова "Свернуть в трей" | UI | PASS |
| TC-10 | Проверка подгрузки .env файла | Переменные окружения прочитаны, API запросы идут на правильный URL | Логи | PASS |
| TC-11 | API: GET /api/health | Код 200, тело {"status":"ok"} | curl | PASS |
| TC-12 | API: GET /api/entries/999999 (несуществующая запись) | Код 404, JSON с ошибкой | curl | PASS |

## 3. Инструменты
- DevTools (в Tauri: F12 или правая кнопка → Inspect)
- curl (встроен в Windows 10/11, Linux, macOS)
- Терминал (для запуска с логами)
- Lighthouse (для веб-версии)

## 4. Итог
Все 12 тестов пройдены. Критических дефектов нет. Найденные ранее проблемы (CORS, .env, иконка, микрофон, трей) исправлены в релизе v0.1.0.
