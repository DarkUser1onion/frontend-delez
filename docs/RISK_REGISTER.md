# RISK_REGISTER.md – Delёz

| ID | Риск | Признак/доказательство | Вероятность | Влияние | Уровень | Мера снижения |
|----|------|------------------------|-------------|---------|---------|---------------|
| R-01 | CORS блокирует запросы из десктоп-приложения | В консоли DevTools ошибки вида "Cross-Origin Request Blocked", приложение не работает | Низкая (исправлено через `@tauri-apps/plugin-http`) | Высокое | Низкий | Использовать плагин для нативных HTTP-запросов; закрепить версию в Cargo.toml; добавить smoke-тест API после сборки |
| R-02 | Утечка секретов (файл .env в репозитории) | .env.example есть, но реальный .env может быть случайно закоммичен (было удалено через filter-branch) | Низкая | Критическое | Средний | .env в .gitignore, использовать .env.example, проверять `git ls-files | grep .env` в CI |
| R-03 | Приложение не запускается на macOS из-за Gatekeeper | Пользователь видит сообщение "программа повреждена", не может открыть .dmg | Средняя | Среднее | Средний | Документировать команду `xattr -d com.apple.quarantine`; в будущем пройти нотаризацию Apple |
| R-04 | Иконка в трее Windows не отображается (если сборка использует .png) | В системном трее пустое место или стандартная иконка | Низкая | Низкое | Низкий | Автоматизировать проверку: при сборке для Windows обязательно наличие .ico; добавить fallback на встроенную иконку |
| R-05 | Голосовой ввод на Linux не работает (неправильное устройство arecord) | Микрофон не записывает, либо пишет с монитора колонок | Низкая (исправлено явным выбором устройства) | Среднее | Низкий | Предустановка корректного устройства в коде; добавить в настройки выбор источника записи |
| R-06 | Окно не открывается из трея после закрытия горячей клавишей (баг исправлен, но может вернуться) | Кнопка в трее показывает "Свернуть в трей", но окно не открывается | Низкая | Среднее | Низкий | Написать интеграционный тест на поведение трея; проверять при каждом релизе |
| R-07 | Не подтягивается .env при запуске (переменные окружения пусты) | API запросы идут на localhost:1420 или падают с ошибкой "missing API_URL" | Низкая | Высокое | Низкий | Исправлен путь загрузки .env; в README указать проверку `console.log(import.meta.env)` |
| R-08 | Недоступность GigaChat API (таймауты, 504) | Чат не отвечает, в логах ошибки соединения | Средняя | Высокое | Средний | Добавить повторные попытки (retry) с экспоненциальной задержкой; fallback-сообщение "ИИ временно недоступен" |
| R-09 | Повреждение node_modules при копировании билда | Приложение не запускается, ошибка «Cannot find module» | Низкая | Среднее | Низкий | В документации указано: всегда выполнять `npm ci` в целевой папке, не копировать node_modules |
| R-10 | Потеря пользовательских данных после обновления приложения | Пропадают записи дневника, настройки | Низкая | Высокое | Средний | Хранить данные в отдельной директории: `~/.config/delez`, `%APPDATA%\delez`; не удалять при деинсталляции |
